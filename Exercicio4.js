const prompt = require('prompt-sync')()

const num = prompt('Type any number: ')
const numero = Number(num)
raiz = Math.sq

if(numero%2==0)
console.log(`It is ${Math.sqrt(numero,2)}`)

if(numero%2==1)
console.log(`It is ${numero**2}`)

